# Favn Features

This file describes the feature set that is implemented in the repository today.

- Only shipped behavior belongs here.
- Future work belongs in `docs/ROADMAP.md`.
- Audit scope for this update: `apps/favn`, `apps/favn_authoring`, `apps/favn_core`, `apps/favn_runner`, `apps/favn_sql_runtime`, `apps/favn_duckdb`, `apps/favn_orchestrator`, `apps/favn_storage_sqlite`, `apps/favn_storage_postgres`, `apps/favn_local`, and `web/favn_web`.

State labels used below:

- `solid but still private-dev`: implemented and well-covered, but not presented in the repo as release-ready.
- `prototype`: implemented and usable for development, but still clearly early or intentionally thin.
- `compatibility-only`: supported mainly to preserve an older/current authoring path, not because it is the preferred future shape.
- `needs hardening`: implemented, but the code/docs/tests still point to important production-readiness gaps.

## Product Boundary Status

Favn uses a manifest-first product architecture with these package/runtime
boundaries:

- `favn` as the primary public dependency, public authoring surface, manifest
  entrypoint owner, `Favn.SQLClient` owner, and public `mix favn.*` entrypoint
  owner
- `favn_duckdb` as an optional DuckDB execution plugin and SQL adapter dependency
  for projects that execute DuckDB-backed work
- `favn_authoring` as the internal authoring and manifest implementation owner
- `favn_local` as the internal local lifecycle, tooling, and packaging implementation owner
- `favn_core` as the shared compiler, manifest, planning, and contracts layer
- `favn_orchestrator` as the control plane and system of record
- `favn_runner` as the execution runtime
- `favn_web` as the separate public web edge

Storage, orchestrator, runner, local, and web apps are runtime/product
internals. Ordinary consumer projects should depend on `favn`, plus optional
plugin packages such as `favn_duckdb` only when they need that execution backend.

The intended stable `v1` API surface covers authoring modules, manifest
compilation/registration inputs, `Favn.SQLClient`, and supported local commands.
Runtime helper functions exposed on `Favn` remain internal/runtime-dependent
until separately documented as stable. Refs:
`docs/production/public_api_boundary.md`.

Implemented boundary outcomes:

- The local lifecycle, packaging, and bootstrap command surface is implemented: `mix favn.install`, `mix favn.dev`, `mix favn.stop`, `mix favn.reload`, `mix favn.status`, `mix favn.logs`, `mix favn.reset`, `mix favn.build.web`, `mix favn.build.orchestrator`, `mix favn.build.runner`, `mix favn.build.single`, `mix favn.bootstrap.single`, and `mix favn.read_doc`.
- Legacy same-BEAM app paths are removed from supported runtime flows: `apps/favn_legacy` and `apps/favn_view` are gone from the umbrella.
- Stable storage adapter entrypoints are restored as `Favn.Storage.Adapter.SQLite` and `Favn.Storage.Adapter.Postgres`.
- SQL adapters now persist canonical inspectable `json-v1` payloads for run snapshots, run events, and scheduler state instead of BEAM term blobs.
- Scheduler state writes now use explicit optimistic versions.
- External Postgres repo mode now caches successful schema-readiness validation instead of repeating readiness checks on every adapter call.
- Pre-closeout SQL rows persisted as BEAM term blobs are intentionally unsupported. Existing persisted runtime state must be reset or recreated when upgrading to the closeout adapters.

## Implemented Today

### Define assets and data relations

- Single Elixir assets are supported, including docs, metadata, dependencies, windows, and owned relations. State: `solid but still private-dev`. Refs: `Favn.Asset`, `apps/favn_authoring/lib/favn/asset.ex`.
- Older multi-function asset modules are still supported through `@asset`, although new code is clearly steered toward `Favn.Asset` or `Favn.MultiAsset`. State: `compatibility-only`. Refs: `Favn.Assets`, `apps/favn_authoring/lib/favn/assets.ex`.
- Generated multi-assets are supported when many assets share one runtime implementation but differ by config, metadata, dependencies, or relation ownership. State: `solid but still private-dev`. Refs: `Favn.MultiAsset`, `apps/favn_authoring/lib/favn/multi_asset.ex`.
- SQL assets are supported with inline SQL or file-backed SQL, compile-time SQL analysis, tracked relation usage, relation-style SQL references, inferred dependencies from owned relation references, and table, view, and limited incremental materializations. DuckDB-backed materialization creates missing target schemas for owned SQL asset relations before creating tables or views. State: `solid but still private-dev`. Refs: `Favn.SQLAsset`, `Favn.SQL`, `apps/favn_authoring/lib/favn/sql_asset.ex`, `apps/favn_authoring/lib/favn/sql.ex`, `apps/favn_duckdb/lib/favn/sql/adapter/duckdb.ex`.
- Public SQL runtime access is supported through `Favn.SQLClient` for connect/query/execute/transaction/relation/columns usage from plain Elixir code against named `Favn.Connection` entries. The shared SQL runtime now includes connection-scoped admission control so adapters can serialize unsafe SQL sessions and operations while capable backends opt into higher or unlimited write concurrency. State: `solid but still private-dev`. Refs: `Favn.SQLClient`, `Favn.SQL.Client`, `Favn.SQL.Admission`, `apps/favn/lib/favn/sql_client.ex`, `apps/favn_sql_runtime/lib/favn/sql/client.ex`.
- DuckDB named connections can run first-class `duckdb_bootstrap` setup before SQLClient or SQL asset execution. The bootstrap path supports allow-listed DuckDB extension install/load, Azure credential-chain secrets, DuckLake attach against PostgreSQL metadata and ADLS data paths, catalog selection with `USE`, structured failed-step diagnostics, and secret redaction. State: `prototype`. Refs: `Favn.SQL.Adapter.DuckDB.bootstrap_schema_field/0`, `apps/favn_duckdb/lib/favn/sql/adapter/duckdb/bootstrap.ex`, `apps/favn_sql_runtime/lib/favn/sql/client.ex`.
- Connection runtime config values may be literal values or manifest-safe runtime config refs such as `Favn.RuntimeConfig.Ref.env!/1` and `secret_env!/1`; refs are resolved before SQL adapter connect and missing required env vars produce structured connection errors without persisting secret values. State: `solid but still private-dev`. Refs: `Favn.Connection.Loader`, `Favn.Connection.Validator`, `Favn.RuntimeConfig.Ref`, `apps/favn_sql_runtime/lib/favn/connection/validator.ex`.
- External source relations can be declared in the catalog without becoming runnable assets. State: `solid but still private-dev`. Refs: `Favn.Source`, `apps/favn_authoring/lib/favn/source.ex`.
- Shared relation defaults can be inherited from module structure. The recommended consumer docs use connection namespaces plus catalog-based data-platform layers, while schema defaults remain available for teams or engines that model layers as schemas. State: `solid but still private-dev`. Refs: `Favn.Namespace`, `apps/favn_authoring/lib/favn/namespace.ex`.
- SQL asset namespace inheritance is resolved during explicit asset/manifest compilation instead of SQL asset module `@before_compile`, so parent namespace modules and child SQL asset modules can compile in the same parallel compiler batch without order-sensitive relation failures. State: `solid but still private-dev`. Refs: `Favn.SQLAsset`, `Favn.SQLAsset.Compiler`, `apps/favn_authoring/lib/favn/sql_asset.ex`, `apps/favn_core/lib/favn/sql_asset/compiler.ex`.
- Named connection definitions are supported as part of the public authoring contract. State: `solid but still private-dev`. Refs: `Favn.Connection`, `apps/favn_authoring/lib/favn/connection.ex`.
- Elixir assets can declare required runtime config with `source_config/2`, `env!/1`, and `secret_env!/1`; manifests persist only required config references and secret flags, while the runner resolves values into `ctx.config` before execution and fails early on missing required env vars. State: `solid but still private-dev`. Refs: `Favn.Asset`, `Favn.RuntimeConfig.Ref`, `Favn.RuntimeConfig.Resolver`, `apps/favn_authoring/lib/favn/asset.ex`, `apps/favn_core/lib/favn/runtime_config/*.ex`, `apps/favn_runner/lib/favn_runner/context_builder.ex`.
- The standalone tutorial includes a canonical source-system raw landing pattern: an Elixir raw asset reads source runtime config from `ctx.config`, calls a narrow source client outside the asset, writes raw JSON rows to DuckDB through `Favn.SQLClient`, and returns structured run metadata with row count, mode, relation, load timestamp, and hashed source identity. State: `solid but still private-dev`. Refs: `examples/basic-workflow-tutorial/lib/favn_reference_workload/warehouse/raw/orders.ex`, `examples/basic-workflow-tutorial/lib/favn_reference_workload/client/fake_api.ex`, `examples/basic-workflow-tutorial/test/duckdb_execution_test.exs`.
- Window helpers are implemented for hourly, daily, monthly, and yearly asset windows plus run-level anchor and runtime windows. Asset window specs can mark a runtime window as required. State: `solid but still private-dev`. Refs: `Favn.Window`, `apps/favn_authoring/lib/favn/window.ex`.

### Select work and compile manifests

- Pipelines are supported with direct asset targets, multi-target shorthands, additive selectors by asset, module, tag, and category, plus config, metadata, schedules, window policies, source, and outputs. Pipeline window policies support hourly, daily, monthly, and yearly operational anchors; manual local runs can submit explicit `--window` requests, and scheduled windowed runs resolve previous-complete-period anchors in the schedule timezone. Invalid scheduled window policy data is logged as an entry-level submission failure instead of crashing the scheduler runtime. Cron schedules support standard 5-field expressions and 6-field expressions with a leading seconds field, and `missed: :all` catch-up is capped per schedule entry per tick to avoid unbounded high-frequency backlog submission. State: `solid but still private-dev`. Refs: `Favn.Pipeline`, `Favn.Window.Policy`, `Favn.Window.Request`, `apps/favn_authoring/lib/favn/pipeline.ex`, `apps/favn_core/lib/favn/window/policy.ex`, `apps/favn_core/lib/favn/window/request.ex`, `apps/favn/test/public_pipeline_parity_test.exs`.
- Reusable named schedules are supported and can be referenced from pipelines. State: `solid but still private-dev`. Refs: `Favn.Triggers.Schedules`, `apps/favn_authoring/lib/favn/triggers/schedules.ex`.
- Public facade APIs can list assets, fetch a compiled asset or pipeline, resolve a pipeline, generate a manifest, build a manifest artifact, serialize it, hash it, validate compatibility, and pin a manifest version. State: `solid but still private-dev`. Refs: `Favn`, `FavnAuthoring`, `apps/favn/lib/favn.ex`, `apps/favn_authoring/lib/favn.ex`.
- Canonical manifest generation is implemented from explicit module lists or app config, including assets, pipelines, schedules, and dependency graph data. State: `solid but still private-dev`. Refs: `Favn.Manifest.Generator`, `apps/favn_core/lib/favn/manifest/generator.ex`.
- Configured authoring modules are resolved reliably in standalone consumer projects even when modules are not preloaded in the VM before authoring/runtime checks run. State: `solid but still private-dev`. Refs: `Favn.Assets.Compiler`, `Favn.Manifest.Generator`, `Favn.Connection.Loader`, `apps/favn_core/lib/favn/assets/compiler.ex`, `apps/favn_core/lib/favn/manifest/generator.ex`, `apps/favn_runner/lib/favn/connection/loader.ex`.
- Stable manifest serialization, hashing, compatibility validation, immutable manifest-version envelopes, and atom-safe manifest rehydration are implemented. Rehydration expects manifest module and atom names to already exist in the current code universe. State: `solid but still private-dev`. Refs: `Favn.Manifest.Serializer`, `Favn.Manifest.Identity`, `Favn.Manifest.Compatibility`, `Favn.Manifest.Version`, `apps/favn_core/test/manifest/version_test.exs`.
- Deterministic graph indexing and planning are implemented, including dependency inclusion modes, topological stages, and expansion of backfill ranges across windowed assets. State: `solid but still private-dev`. Refs: `Favn.Assets.GraphIndex`, `Favn.Assets.Planner`, `apps/favn_core/lib/favn/assets/planner.ex`, `apps/favn_core/test/assets/graph_planner_parity_test.exs`.

### Run pinned work

- A separate runner boundary is implemented. The runner can register pinned manifest versions, accept asynchronous work, wait for results, cancel work, and run synchronously through the same boundary. The local-node runner client normalizes local and remote dispatch exceptions, exits, and RPC failures into explicit `{:error, reason}` tuples at the orchestrator boundary. State: `solid but still private-dev`. Refs: `FavnRunner`, `FavnRunner.Server`, `FavnOrchestrator.RunnerClient.LocalNode`, `apps/favn_runner/lib/favn_runner.ex`, `apps/favn_runner/lib/favn_runner/server.ex`, `apps/favn_orchestrator/lib/favn_orchestrator/runner_client/local_node.ex`.
- Elixir assets execute through the runner using manifest-pinned runtime context instead of ad hoc orchestrator-side module discovery. State: `solid but still private-dev`. Refs: `apps/favn_runner/test/favn_runner_test.exs`, `apps/favn_orchestrator/test/orchestrator_runner_integration_test.exs`.
- Source assets are treated as observe/no-op nodes in execution. State: `solid but still private-dev`. Refs: `apps/favn_runner/test/favn_runner_test.exs`, `Favn.Assets.Planner`.
- SQL assets execute from manifest-carried SQL payloads through a runner-owned materialization planner and the shared SQL runtime client, and the runner does not fall back to compiled modules when manifest data is missing. State: `needs hardening`. Refs: `FavnRunner.SQL.MaterializationPlanner`, `apps/favn_runner/test/execution/sql_asset_test.exs`, `apps/favn_runner/test/sql/materialization_planner_test.exs`, `apps/favn_core/lib/favn/manifest/sql_execution.ex`, `apps/favn_sql_runtime/lib/favn/sql/client.ex`.
- Runner-side cancellation, timeout handling, and crash reporting are implemented. State: `solid but still private-dev`. Refs: `apps/favn_runner/test/server_test.exs`, `apps/favn_runner/lib/favn_runner/server.ex`.
- DuckDB is implemented as runner/plugin-owned data-plane infrastructure with both in-process and separate-process execution modes. Local-file DuckDB connections default to single-admitted SQL sessions against the same database path; production local-file paths must be explicit and durable, while DuckLake-style configurations can opt into unlimited write concurrency through connection config. Separate-process mode is implemented and is the recommended production placement for keeping DuckDB handles in a supervised worker process instead of the asset worker process. In separate-process mode, worker unavailability produces retryable structured SQL diagnostics, while worker-call timeouts are reported as non-retryable unknown-outcome failures; a successful appender close consumes the worker handle, while a failed close leaves the handle retryable or explicitly releasable; adapter-owned appender materialization releases the handle after failed closes. State: `prototype`. Refs: `FavnDuckdb`, `FavnDuckdb.Runtime`, `FavnDuckdb.Worker`, `Favn.SQL.Adapter.DuckDB`, `apps/favn_duckdb/lib/favn_duckdb.ex`, `apps/favn_duckdb/test/favn_duckdb_test.exs`, `apps/favn_duckdb/test/sql/adapter/duckdb_hardening_test.exs`, `docs/production/single_node_contract.md`.

### Orchestrate and operate runs

- Manifest storage is implemented in the orchestrator, including registration, listing, activation, active-manifest lookup, and manifest-scoped run targets. State: `solid but still private-dev`. Refs: `FavnOrchestrator`, `apps/favn_orchestrator/lib/favn_orchestrator.ex`.
- Asset runs and pipeline runs are submitted as manifest-pinned runs, and reruns stay pinned to the source run's manifest version. State: `solid but still private-dev`. Refs: `FavnOrchestrator`, `apps/favn_orchestrator/test/orchestrator_runner_integration_test.exs`.
- Run state views, run history, persisted run events, cancellation, and rerun flows are implemented. Duplicate run-event writes are idempotent when the normalized event content is identical for the same `{run_id, sequence}`, and conflicting when the sequence is reused for different content. State: `solid but still private-dev`. Refs: `FavnOrchestrator`, `FavnOrchestrator.Projector`, `Favn.Storage.Adapter`, `apps/favn_orchestrator/lib/favn_orchestrator.ex`.
- Run detail and active-manifest asset target API payloads expose per-asset result metadata, relation identity, dependencies, runtime config declarations, windows, and raw run context needed by the local web inspector. State: `solid but still private-dev`. Refs: `FavnOrchestrator.API.Router`, `apps/favn_orchestrator/test/api/router_test.exs`.
- Safe local landed-data inspection is implemented for manifest-owned asset relations through a runner-owned inspection command and private orchestrator/web BFF endpoints. The first DuckDB-backed path supports relation lookup, schema columns, row count, capped sample rows, and safe table metadata without exposing a general SQL editor. State: `prototype`. Refs: `Favn.Contracts.RelationInspectionRequest`, `FavnRunner.Inspection`, `Favn.SQL.Client`, `Favn.SQL.Adapter.DuckDB`, `apps/favn_orchestrator/lib/favn_orchestrator/api/router.ex`, `web/favn_web/src/routes/api/web/v1/manifests/[manifest_version_id]/assets/[target_id]/inspection/+server.ts`.
- Operational-backfill foundations are implemented in the orchestrator: pure explicit and relative range resolution, parent backfill runs, child pipeline runs per resolved window, normalized backfill-window ledger rows, latest asset/window state, safe coverage-baseline projection from successful run metadata, private HTTP endpoints to submit backfills, list windows, rerun a failed window, list coverage baselines, list asset/window state, and repair/replay derived backfill projections from authoritative run snapshots, a local `mix favn.backfill` operator task for those endpoints, and a local web operator surface for active-manifest explicit range submission, parent-window inspection, failed-window rerun, coverage-baseline browsing/selection, and asset/window state browsing. Backfill read endpoints are bounded with default `limit=100`, maximum `limit=500`, `offset` pagination, response pagination metadata, and explicit allow-lists for status values plus manifest-owned pipeline/asset filters. Projection repair is dry-run by default and only mutates derived read models when `--apply` or `apply: true` is provided; it never mutates authoritative run snapshots or run events. Parent-run scoped repair rebuilds the parent window ledger only, while pipeline/full repair also recomputes latest asset/window state. Lookback-policy input is not accepted by operational backfill until concrete runtime semantics exist. State: `prototype`. Refs: `Favn.Backfill.RangeRequest`, `Favn.Backfill.RangeResolver`, `FavnOrchestrator.submit_pipeline_backfill/2`, `FavnOrchestrator.submit_pipeline_backfill_for_manifest/3`, `FavnOrchestrator.rerun_backfill_window/4`, `FavnOrchestrator.repair_backfill_projections/1`, `FavnOrchestrator.API.Router`, `FavnOrchestrator.Backfill.Repair`, `FavnOrchestrator.Backfill.BackfillWindow`, `FavnOrchestrator.Backfill.AssetWindowState`, `FavnOrchestrator.Backfill.CoverageBaseline`, `apps/favn/lib/mix/tasks/favn.backfill.ex`, `apps/favn_local/lib/favn/dev/backfill.ex`, `apps/favn_orchestrator/priv/http_contract/v1/*backfill*.schema.json`, `web/favn_web/src/routes/backfills/**`, `web/favn_web/src/routes/assets/window-states/**`.
- Schedule inspection is implemented from the active manifest, with runtime reload and tick hooks when the scheduler process is running. State: `solid but still private-dev`. Refs: `FavnOrchestrator.reload_scheduler/0`, `FavnOrchestrator.tick_scheduler/0`, `FavnOrchestrator.list_schedule_entries/0`, `apps/favn_orchestrator/lib/favn_orchestrator.ex`.
- Live run updates are implemented with global and run-scoped SSE endpoints, including replay-after-cursor for run streams. State: `prototype`. Refs: `FavnOrchestrator.API.Router`, `FavnOrchestrator.list_run_stream_events/2`, `apps/favn_orchestrator/test/api/router_test.exs`.
- A private orchestrator HTTP API is implemented for manifests, runs, operational backfills, schedules, auth, actor management, audit reads, and SSE streams. State: `solid but still private-dev`. Refs: `apps/favn_orchestrator/lib/favn_orchestrator/api/router.ex`, `apps/favn_orchestrator/priv/http_contract/v1/*.schema.json`.
- The Phase 1 production runtime config contract is validated across the first single-node shape: orchestrator validates SQLite storage/API/service-token/scheduler/local-runner client config before serving traffic, runner validates `FAVN_RUNNER_MODE=local`, web validates its orchestrator URL/service token/session secret before production request handling, and orchestrator readiness delegates storage diagnostics through the storage adapter boundary while isolating check failures as redacted `503` diagnostics. State: `prototype`. Refs: `FavnOrchestrator.ProductionRuntimeConfig`, `FavnOrchestrator.Readiness`, `FavnRunner.ProductionRuntimeConfig`, `web/favn_web/src/lib/server/runtime_config.ts`, `apps/favn_orchestrator/test/production_runtime_config_test.exs`, `apps/favn_orchestrator/test/readiness_test.exs`, `web/favn_web/src/lib/server/runtime_config.spec.ts`.
- Local username/password auth, actor roles (`viewer`, `operator`, `admin`), session introspection and revocation, and audit logging are implemented in the orchestrator. State: `needs hardening`. Refs: `FavnOrchestrator.Auth`, `apps/favn_orchestrator/lib/favn_orchestrator/api/router.ex`, `apps/favn_orchestrator/test/api/router_test.exs`.

### Store runtime state

- In-memory orchestrator storage is implemented and is the default local mode. State: `solid but still private-dev`. Refs: `FavnOrchestrator.Storage.Adapter.Memory`, `apps/favn_orchestrator/lib/favn_orchestrator.ex`.
- SQLite control-plane persistence is implemented for runs, events, scheduler state, write ordering, optimistic scheduler versions, and canonical `json-v1` payload storage. The adapter now has internal database-path diagnostics, schema diagnostics that distinguish empty/current/missing/upgrade-required/newer/inconsistent schemas, manual startup failure for non-ready existing schemas, explicit empty-DB initialization support, and stopped-backend restore verification coverage for manifests, active manifest selection, runs, run events, scheduler state, and operational-backfill read models. State: `solid but still private-dev`. Refs: `Favn.Storage.Adapter.SQLite`, `FavnStorageSqlite.Diagnostics`, `FavnStorageSqlite.Migrations`, `apps/favn_storage_sqlite/test/sqlite_storage_test.exs`, `apps/favn_storage_sqlite/test/sqlite_readiness_test.exs`, `apps/favn_storage_sqlite/test/sqlite_control_plane_restore_test.exs`.
- Postgres control-plane persistence is implemented with managed-repo and external-repo modes, cached external readiness validation, and canonical `json-v1` payload storage. State: `solid but still private-dev`. Refs: `Favn.Storage.Adapter.Postgres`, `apps/favn_storage_postgres/test/adapter_test.exs`.
- Storage adapter startup returns `{:ok, child_spec}`, `:none`, or `{:error, reason}` for recoverable configuration errors, and scheduler state keys are exact across memory, SQLite, and Postgres: `{pipeline_module, nil}` stores or fetches the nil schedule id and does not fall back to a concrete schedule id. State: `solid but still private-dev`. Refs: `Favn.Storage.Adapter`, `apps/favn_orchestrator/test/integration/storage_adapter_contract_test.exs`.
- Memory, SQLite, and Postgres storage now persist normalized operational-backfill read models for coverage baselines, backfill windows, and latest asset/window state, with consistent bounded pagination semantics for read-model lists. State: `prototype`. Refs: `Favn.Storage.Adapter`, `FavnOrchestrator.Storage`, `apps/favn_storage_sqlite/lib/favn_storage_sqlite/migrations/add_backfill_state.ex`, `apps/favn_storage_postgres/lib/favn_storage_postgres/migrations/add_backfill_state.ex`.

### Develop locally

- Public Mix tasks exist for `init`, `doctor`, `install`, `dev`, `run`, `backfill`, `status`, `logs`, `reload`, `stop`, `reset`, `build.runner`, `build.web`, `build.orchestrator`, `build.single`, and `bootstrap.single`. They reject unsupported options and unexpected positional arguments before performing work. State: `solid but still private-dev`. Refs: `apps/favn/lib/mix/tasks/*.ex`, `apps/favn/test/mix_tasks/public_tasks_test.exs`.
- `mix favn.init --duckdb --sample` bootstraps a standalone Mix consumer project with a local DuckDB smoke path: generated connection and namespace modules, one raw Elixir asset, one downstream SQL business output, a `deps(:all)` pipeline, local Favn config, `.env.example`, and an idempotent local `favn_duckdb` dependency insertion when the standard `defp deps/0` shape is recognized. `mix favn.doctor` validates the local setup before `install/dev/run`. State: `solid but still private-dev`. Refs: `Favn.Dev.Init`, `Favn.Dev.Doctor`, `apps/favn_local/lib/favn/dev/init.ex`, `apps/favn_local/lib/favn/dev/doctor.ex`, `apps/favn_local/test/dev_init_test.exs`, `apps/favn_local/test/dev_doctor_test.exs`.
- Private local consumer projects can combine `favn` with optional plugin app path dependencies from one local checkout, for example `favn_duckdb`, without manually listing internal runtime apps. State: `solid but still private-dev`. Refs: `apps/favn/test/consumer_dependency_install_test.exs`, `apps/*/mix.exs`.
- Local compiled-document lookup is implemented through `mix favn.read_doc` for modules and public functions. State: `solid but still private-dev`. Refs: `apps/favn/lib/mix/tasks/favn.read_doc.ex`, `apps/favn/test/mix_tasks/read_doc_task_test.exs`.
- AI-oriented doc breadcrumbs now exist in compiled docs through `Favn.AI`, including a concise recommended consumer project shape, richer `Favn` function docs, and cross-links on the main authoring/local-tooling entry modules. State: `solid but still private-dev`. Refs: `apps/favn/lib/favn/ai.ex`, `apps/favn/lib/favn.ex`, `apps/favn_authoring/lib/favn/*.ex`, `apps/favn_local/lib/favn/dev.ex`, `apps/favn_core/lib/favn/manifest/generator.ex`.
- `mix favn.install` fingerprints toolchain inputs, consumer lock state, and the copied runtime source tree, materializes an install-owned runtime workspace under `.favn/install/runtime_root`, records runtime metadata under `.favn/install/runtime.json`, and installs runtime/web dependencies for local tooling. Source-only Favn runtime updates refresh the installed runtime while generated dependency/build directories are excluded from the source-tree hash through the same runtime tree policy used for materialization. State: `solid but still private-dev`. Refs: `Favn.Dev.Install`, `Favn.Dev.RuntimeSource`, `Favn.Dev.RuntimeTreePolicy`, `apps/favn_local/lib/favn/dev/install.ex`, `apps/favn_local/lib/favn/dev/runtime_source.ex`, `apps/favn_local/lib/favn/dev/runtime_tree_policy.ex`, `apps/favn_local/test/dev_install_test.exs`, `apps/favn_local/test/dev_runtime_source_test.exs`.
- `mix favn.dev` starts runner, orchestrator, and web as separate local processes, stores runtime state under `.favn/runtime.json`, checks HTTP-level readiness through a dependency-free loopback-only local HTTP client, validates Distributed Erlang node names and RPC cookies before atom conversion, disables the local scheduler by default unless `--scheduler` or local config opts in, forwards only the supported `FAVN_ORCHESTRATOR_BOOTSTRAP_*` keys from the consumer `.env` or shell environment to the orchestrator process, passes only explicit local-dev consumer Favn config keys into the runner through a bounded tagged transport with explicit top-level key mapping, validated module/local atom recreation, consumer-root-relative database paths, and redacted diagnostics, strictly accepts only positive-integer local port and Postgres port/pool-size config values, preserves corrupt or unreadable `.favn/secrets.json` errors instead of silently rotating generated local credentials, starts service wrappers without linking them to callers, rejects services whose OS pid cannot be reported, handles service log-write failures as explicit service exits, and bootstraps one pinned manifest version into both runner and orchestrator with structured local API failure diagnostics. State: `solid but still private-dev`. Refs: `Favn.Dev.Config`, `Favn.Dev.Secrets`, `Favn.Dev.DistributedErlang`, `Favn.Dev.Stack`, `Favn.Dev.Process`, `Favn.Dev.LocalHttpClient`, `Favn.Dev.RuntimeLaunch`, `Favn.Dev.ConsumerConfigTransport`, `Favn.Manifest.Version`, `apps/favn_local/lib/favn/dev/config.ex`, `apps/favn_local/lib/favn/dev/secrets.ex`, `apps/favn_local/lib/favn/dev/distributed_erlang.ex`, `apps/favn_local/lib/favn/dev/stack.ex`, `apps/favn_local/lib/favn/dev/process.ex`, `apps/favn_local/lib/favn/dev/local_http_client.ex`, `apps/favn_local/lib/favn/dev/runtime_launch.ex`, `apps/favn_local/lib/favn/dev/consumer_config_transport.ex`, `apps/favn_core/lib/favn/manifest/version.ex`.
- `mix favn.run PipelineModule` submits a pipeline run to the running local stack through the private orchestrator HTTP boundary, using generated local operator credentials and active-manifest target resolution. Windowed pipeline runs can pass `--window hour:YYYY-MM-DDTHH`, `--window day:YYYY-MM-DD`, `--window month:YYYY-MM`, or `--window year:YYYY`, with optional `--timezone`. State: `solid but still private-dev`. Refs: `Mix.Tasks.Favn.Run`, `Favn.Dev.Run`, `apps/favn/lib/mix/tasks/favn.run.ex`, `apps/favn_local/lib/favn/dev/run.ex`.
- `mix favn.backfill` submits, inspects, reruns, and repairs operational backfills against the running local stack through the private orchestrator HTTP boundary, using generated local operator credentials and active-manifest pipeline target resolution. It supports `submit` with explicit `--from`, `--to`, `--kind`, and optional `--timezone`, `windows RUN_ID`, `rerun-window RUN_ID --window-key KEY`, `coverage-baselines`, `asset-window-states`, and dry-run-by-default `repair` with optional `--backfill-run-id`, `--pipeline-module`, and `--apply`; read commands accept `--limit` and `--offset` and print a next-page hint when more rows are available. State: `prototype`. Refs: `Mix.Tasks.Favn.Backfill`, `Favn.Dev.Backfill`, `Favn.Dev.OrchestratorClient`, `apps/favn/lib/mix/tasks/favn.backfill.ex`, `apps/favn_local/lib/favn/dev/backfill.ex`.
- Local storage selection is implemented for memory, SQLite, and Postgres. State: `solid but still private-dev`. Refs: `apps/favn_local/README.md`, `apps/favn_local/test/integration/dev_storage_verification_test.exs`.
- `mix favn.status`, `mix favn.logs`, `mix favn.stop`, and `mix favn.reset` are implemented for stack inspection, log reading, shutdown, and local state cleanup. State: `solid but still private-dev`. Refs: `apps/favn_local/test/dev_logs_test.exs`, `apps/favn_local/test/dev_reset_test.exs`, `apps/favn/test/mix_tasks/public_tasks_test.exs`.
- Local build flows are implemented for runner, web, orchestrator, and single-node outputs. State: `prototype` overall. `build.runner` includes the pinned manifest plus compiled beams from the current project app, `build.single` now emits a project-local backend-only SQLite launcher with executable `bin/start|stop` but is not marked operational because it still depends on the installed runtime source root and lacks executed artifact start/stop verification, and `build.web`/`build.orchestrator` remain metadata-oriented outputs. Refs: `apps/favn_local/test/dev_build_runner_test.exs`, `apps/favn_local/test/dev_build_web_test.exs`, `apps/favn_local/test/dev_build_orchestrator_test.exs`, `apps/favn_local/test/dev_build_single_test.exs`.
- `mix favn.bootstrap.single` implements the CLI/artifact-side Phase 1 single-node backend bootstrap workflow through orchestrator APIs: required manifest/orchestrator URL/service-token inputs can come from flags or supported environment variables, service-token auth is verified through `/api/orchestrator/v1/bootstrap/service-token`, manifest JSON is read and verified as a stable `Favn.Manifest.Version`, the manifest is registered, activation runs by default, and `/api/orchestrator/v1/manifests/:manifest_version_id/runner/register` asks the orchestrator to register the persisted manifest with the local runner. Identical manifest registration and runner registration are safe/repeatable, and the final summary states whether active-manifest verification was possible. This Phase 1 bootstrap deliberately excludes durable browser login, durable sessions, actors, credentials, and audit logs; that remains follow-up auth/session/audit work. State: `prototype`. Refs: `Mix.Tasks.Favn.Bootstrap.Single`, `Favn.Dev.Bootstrap.Single`, `Favn.Dev.OrchestratorClient`, `apps/favn/test/mix_tasks/public_tasks_test.exs`, `apps/favn_local/test/dev_bootstrap_single_test.exs`, `apps/favn_local/test/dev_orchestrator_client_test.exs`, `apps/favn_orchestrator/test/api/router_test.exs`.
- SQLite single-node bootstrap acceptance coverage verifies the backend control-plane persistence slice across manifest registration, active-manifest selection, scheduler state persistence, manifest-with-runner registration, and storage restart survival. It intentionally stops at ready-for-run-submission and excludes auth/session/audit durability. State: `prototype`. Refs: `apps/favn_storage_sqlite/test/sqlite_single_node_bootstrap_acceptance_test.exs`.
- A standalone basic workflow tutorial project is available under `examples/basic-workflow-tutorial`, using local path dependencies to exercise Favn authoring, manifest generation, and DuckDB execution like a consumer project outside the umbrella. State: `solid but still private-dev` for compile/test coverage; the embedded `mix favn.dev` loop is tracked separately. Refs: `examples/basic-workflow-tutorial/README.md`, `examples/basic-workflow-tutorial/test/`.

### Use the web prototype

- A separate SvelteKit web/BFF workspace is implemented under `web/favn_web`. It handles signed cookie sessions, login/logout through orchestrator-owned password auth, protected pages, server-side relays to the private orchestrator API, and production validation for web-to-orchestrator URL/service-token plus web session secret config. State: `needs hardening`. Refs: `web/favn_web/src/hooks.server.ts`, `web/favn_web/src/lib/server/session.ts`, `web/favn_web/src/lib/server/orchestrator.ts`, `web/favn_web/src/lib/server/runtime_config.ts`.
- The authenticated web landing path redirects to `/runs`, a local run inspector with a control-room app shell, status-filtered runs table, manual active-manifest pipeline submission card with hour/day/month/year window controls, failed-run detail overview, asset table, outputs table, timeline, manifest/window context, raw payload view, and asset detail debugging sheet. State: `prototype`. Refs: `web/favn_web/src/routes/runs/**`, `web/favn_web/src/lib/components/favn/RunDetailPage.svelte`, `web/favn_web/src/lib/components/favn/RunsPage.svelte`, `web/favn_web/src/lib/pipeline_run_submission.ts`, `web/favn_web/src/lib/server/run_views.ts`.
- The local web UI includes an asset catalog at `/assets` and asset details at `/assets/[asset_ref]`, backed by active-manifest target data and recent runs. It supports client-side search/filtering, summary counts, asset-scoped run history, latest materialization summaries, declared runtime config display with secret flags, safe relation previews for schema/row count/sample rows where supported, honest unavailable/error states, and a run-with-upstream action using the current orchestrator asset submission path. State: `prototype`. Refs: `web/favn_web/src/routes/assets/**`, `web/favn_web/src/lib/components/favn/AssetCatalogPage.svelte`, `web/favn_web/src/lib/components/favn/AssetDetailPage.svelte`, `web/favn_web/src/lib/server/asset_catalog_views.ts`.
- Web BFF endpoints exist for runs, manifests, schedules, run-stream relays, manifest-owned asset inspection relays, and operational-backfill submit/read/rerun relays. Pipeline run submissions can forward explicit window requests, backfill submissions can forward explicit range requests with optional coverage baselines, and run details display projected pipeline/anchor/runtime window context when the orchestrator payload exposes it. State: `prototype`. Refs: `web/favn_web/src/routes/api/web/v1/**`, `web/favn_web/src/lib/pipeline_run_submission.ts`, `web/favn_web/src/lib/backfill_submission.ts`, `web/favn_web/src/lib/server/run_views.ts`, `web/favn_web/src/lib/server/backfill_views.ts`, `web/favn_web/src/lib/components/favn/RunDetailPage.svelte`, `web/favn_web/src/lib/components/favn/BackfillsPage.svelte`, `web/favn_web/tests/e2e/auth-session-runs.e2e.ts`.
- End-to-end browser tests cover login, logout, unauthorized redirects, and basic runs, manifests, schedules, and run-stream relay behavior. State: `solid but still private-dev` as test coverage, but it covers a thin prototype rather than a mature product UI. Refs: `web/favn_web/tests/e2e/auth-session-runs.e2e.ts`.

## Current Caveats And Partial Areas

- No major area in this repository is currently documented as fully release-ready. The strongest areas today are authoring, manifest compilation, planning, runner and orchestrator basics, and the local storage loops, but the project still presents itself as private development.
- The first `v1` production deployment contract is documented as a single backend node with SQLite control-plane persistence on durable attached storage and runner/plugin-owned DuckDB data-plane execution. Some packaging, backup automation, auth/session, web, and operator-readiness work remains follow-up. Refs: `docs/production/single_node_contract.md`.
- The public package and API support boundary is documented for `v1`: `favn` is the primary public dependency, `favn_duckdb` is optional for DuckDB execution, internal runtime/product apps are not ordinary user dependencies, and runtime helpers on `Favn` are not stable `v1` API. Refs: `docs/production/public_api_boundary.md`.
- The web surface is a working prototype, not a polished product UI. The clearest evidence is the thin BFF route set and the current E2E coverage, not the default scaffold `web/favn_web/README.md`, which is still outdated.
- The orchestrator HTTP API is private service-to-service infrastructure, not a documented public external API contract.
- The public `Favn` module still contains thin runtime delegation helpers. Runtime-facing calls such as `run_pipeline`, `get_run`, and scheduler helpers depend on runtime apps being available and return `:runtime_not_available` when they are not; they are internal/runtime-dependent conveniences, not stable `v1` API. Refs: `apps/favn/lib/favn.ex`, `apps/favn/test/runtime_facade_test.exs`, `docs/production/public_api_boundary.md`.
- Auth, session, and audit foundations exist now, but the current implementation is still prototype-grade: the orchestrator auth store is in-memory, browser-edge abuse controls are not present, and service credentials are still a simple static-token model. Refs: `apps/favn_orchestrator/lib/favn_orchestrator/auth/store.ex`, `apps/favn_orchestrator/lib/favn_orchestrator/api/router.ex`, `docs/REFACTOR.md`.
- SSE support is uneven today. Run-scoped replay is the strongest implemented path, while the global runs stream is still much thinner and not yet a durable or scalable live-update contract. Refs: `apps/favn_orchestrator/lib/favn_orchestrator/api/router.ex`, `apps/favn_orchestrator/test/api/router_test.exs`, `docs/REFACTOR.md`.
- Packaging is uneven today. `build.single` has project-local backend launcher scripts but is not a self-contained operational artifact, and `build.runner` carries the strongest project-code package contract, while `build.web` and `build.orchestrator` remain metadata-oriented rather than standalone production deployment artifacts. Refs: `apps/favn_local/test/dev_build_runner_test.exs`, `apps/favn_local/test/dev_build_web_test.exs`, `apps/favn_local/test/dev_build_orchestrator_test.exs`, `apps/favn_local/test/dev_build_single_test.exs`.
- Runtime config refs for Elixir assets are resolved before asset invocation. Connection runtime config refs are resolved when SQL connection loading/connect occurs; a full run-level preflight that validates all SQL connection refs before any planned asset starts is still planned. Refs: `apps/favn_runner/lib/favn_runner/context_builder.ex`, `apps/favn_sql_runtime/lib/favn/connection/validator.ex`, `docs/ROADMAP.md`.
- Landed-data inspection is intentionally curated and local/prototype-grade. It inspects manifest-owned relations through runner-owned read-only operations and caps samples at 20 rows; it is not a general SQL editor or a production data-access policy. Refs: `docs/ISSUE_172_LOCAL_INSPECTION_PANEL_PLAN.md`.
- Persisted runtime state from pre-closeout SQL adapters is not upgrade-compatible. Older SQLite or Postgres rows stored as BEAM term blobs must be reset or recreated once before using the closeout adapters. Refs: `README.md`, `docs/REFACTOR.md`.
- Postgres support is implemented at the adapter and configuration level, but broader live verification is still opt-in in tests. Refs: `apps/favn_storage_postgres/test/adapter_test.exs`, `apps/favn_storage_postgres/test/integration/adapter_live_test.exs`, `docs/REFACTOR.md`.

## Not In Scope For This File

- Planned next work and later ideas live in `docs/ROADMAP.md`.
